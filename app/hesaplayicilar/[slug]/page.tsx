import { Nav } from '@/components/ui/Nav';import { calculators, safetyNotice } from '@/lib/data';
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=calculators.find(x=>x.slug===slug)??calculators[0];return <><Nav/><div className="container"><h1>{c.title}</h1><p>{c.desc}</p><p className="warn">{safetyNotice}</p><form className="form"><label>Örnek giriş 1<input type="number" min="0" placeholder="kg, V veya W"/></label><label>Örnek giriş 2<input type="number" min="0" placeholder="motor sayısı, Ah veya rezerv"/></label><button className="btn" type="button">Hesapla ve proje geçmişine kaydet</button></form><div className="card"><h2>Sonuç kartı</h2><p>Sunucu API hesaplamayı doğrular; formül, yorum, uyarı ve yazdırma aksiyonu burada gösterilir.</p></div></div></>}
