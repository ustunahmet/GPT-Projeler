import { NextResponse } from 'next/server';export async function GET(){return NextResponse.json({success:true,data:[]})}export async function POST(req:Request){const b=await req.json();if(!b.name||!b.drone_type||!b.motor_count||!b.auw_kg)return NextResponse.json({success:false,error:{code:'VALIDATION_ERROR',message:'Proje adı, drone tipi, motor sayısı ve AUW zorunludur.',details:[]}},{status:400});return NextResponse.json({success:true,data:{id:'demo-project',...b,status:'active'}})}
